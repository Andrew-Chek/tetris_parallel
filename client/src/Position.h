#ifndef POSITION_H
#define POSITION_H

class Position {
public:
    int x, y;

    // Constructor with default values
    Position(int x = 0, int y = 0);

    // Optional: Member functions to set and get x and y could be added if needed.
};

#endif // POSITION_H
